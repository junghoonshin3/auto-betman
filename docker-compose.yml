services:
  betman:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      FAKE_PURCHASES_FILE: /app/storage/fake_purchases.json
    volumes:
      - ./storage:/app/storage
